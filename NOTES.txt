req = request. Represents the HTTP request the client sends to the server

res = response. It represents the HTTP response that the server sends back to the client.

next = function. Hands off control to the next middleware function.
When called, Express will move to the next middleware function or route handler in the chain.